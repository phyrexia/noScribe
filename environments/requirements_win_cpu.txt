# Install with `pip install -r requirements_win_cpu.txt`
# torchaudio==2.8 — pyannote 4 is not compatible with torch 2.9; torch version
# is pinned via torchaudio.
torchaudio==2.8

AdvancedHTMLParser
anthropic
appdirs
cpufeature
customtkinter
faster-whisper
pyannote.audio>=4.0
Pillow
python-i18n
PyYAML
sounddevice
soundfile

# Build / packaging.
# Pinned: 6.14.1's bootloader is less likely to trip AV false positives than
# newer releases.
pyinstaller==6.14.1
